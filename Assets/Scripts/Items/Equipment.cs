using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Equipment : Item {
	/*
	public string slot;
	public Dictionary<Stat, int> statsModified = new Dictionary<Stat, int>();
	public Unit equippedUnit;
	
	// Use this for initialization
	void Start () {
		foreach (Stat s in statsModified.Keys)
		{
			equippedUnit.ModifyStat(s, statsModified[s], gameObject.name);
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	*/
}
